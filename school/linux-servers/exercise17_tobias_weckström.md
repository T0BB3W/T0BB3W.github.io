# exercise17_tobias_weckström

1 (1)
Tobias Weckström                                         Exercise 17
Haaga-Helia University of Applied Sciences               9.3.2026
ICI003AS2AE-3006



Exercise

            SSH Server is done. And I was even able to get ssh and apache working on my laptop, I
            just had to manually change the mac address in the bridged network adapter in
            virtualbox settings to the mac address of the appropriate network mac address in my
            system.

            Tree:




            Here it is illustrated quite easily, the main point is you can navigate to directories on the
            same level, for example from dir1 to dir2 by just using cd dir2, but if you are trying to go
            from dir1dir1 to dir2 it wont work, and you have to write out the path from the parent.
            And the image is a little bit misleading since above all should be root.

            Installed nethack using ‘sudo apt install nethack-console’ after which it can be played
            using ‘nethack’ command

            Information on the packages can be found using man command or –help for quick
            reference. Also can use info or whatis command. Packages come from repositories.

            Finding packages by using ‘dpkg -l’ lists all packages but if you want to find a certain one
            or just remember part of the name use ‘dpkg -l | grep keyword’
